name: Update Kubernetes Manifests

on:
  workflow_run:
    workflows: ["Smart Build Pipeline"]
    types:
      - completed
    branches: [main]
  # Also allow manual trigger for testing
  workflow_dispatch:

jobs:
  update-manifests:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
      
      - name: Update image tags
        run: |
          IMAGE_TAG="${{ github.sha }}"
          
          # Update base deployment (Kustomize sẽ override trong overlays)
          yq eval ".spec.template.spec.containers[0].image = \"eshelf/api-gateway:${IMAGE_TAG}\"" \
            -i infrastructure/kubernetes/base/api-gateway-deployment.yaml
          
          # Update other services if they were built
          # Note: Kustomize overlays use patches, so we update base manifests
          # The overlays will apply patches on top of these base images
      
      - name: Commit changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add infrastructure/kubernetes/
          git diff --quiet && git diff --staged --quiet || git commit -m "chore: update image tags to ${{ github.sha }}"
          git push

